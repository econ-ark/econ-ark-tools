chroot /target curl -L -o /var/local/start.sh / ;     chroot /target curl -L -o /var/local/finish.sh / ;     chroot /target curl -L -o /var/local/ / ;     chroot /target curl -L -o /etc/rc.local / ;     chroot /target curl -L -o /var/local/Econ-ARK.VolumeIcon.icns /Disk/Icons/Econ-ARK.VolumeIcon.icns ;     chroot /target curl -L -o /var/local/Econ-ARK.disk_label      /Disk/Labels/Econ-ARK.disklabel    ;     chroot /target curl -L -o /var/local/Econ-ARK.disk_label_2x   /Disk/Labels/Econ-ARK.disklabel_2x ;     chroot /target chmod +x /var/local/start.sh ;     chroot /target chmod +x /var/local/finish.sh ;     chroot /target chmod +x /var/local/ ;     chroot /target chmod +x /etc/rc.local ;     chroot /target mkdir -p /etc/lightdm/lightdm.conf.d ;     chroot /target curl -L -o /etc/lightdm/lightdm.conf.d/autologin-econ-ark.conf /root/etc/lightdm/lightdm.conf.d/autologin-econ-ark.conf ;     chroot /target chmod 755  /etc/lightdm/lightdm.conf.d/autologin-econ-ark.conf ;\ 
     chroot /target curl -L -o /var/local/bash_aliases-add /bash_aliases-add ;
